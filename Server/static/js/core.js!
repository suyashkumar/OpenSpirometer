var spiroApp = angular.module('spiroapp', []);

spiroApp.config(function($routeProvider){
	$routeProvider.when('/',{
		templateUrl : 'pages/home.html',
		controller : 'mainController
	});

});

// Set up Controllers:
spiroApp.controller("mainController", function($scope,$http){ 

 });

